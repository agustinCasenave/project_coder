<h1>HOME</h1>
{{#if products.length}}
    {{#each products}}
    <div>
        <p>Id: {{id}}</p>
        <p>Producto: {{title}}</p>
        <p>Descripción: {{description}}</p>
        <p>Código: {{code}}</p>
        <p>Precio: {{price}}</p>
        <p>Estado: {{status}}</p>
        <p>Stock: {{stock}}</p>
        <p>Categoría: {{category}}</p>
        <p>Thumbnails: {{thumbnails}}</p>
    <br>
    </div>
    {{/each}}
{{else}}
    <p>No existen productos!</p>
{{/if}}